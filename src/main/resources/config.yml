# CustomMenus — конфигурация

# Если true — команда /cm заблокирована для обычных игроков.
# Магазин открывается ТОЛЬКО через NPC.
# Администраторы (custommenu.admin) могут использовать /cm всегда.
npc-only: false
